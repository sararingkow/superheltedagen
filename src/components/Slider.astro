---

---

<div class="slider-container">
  <div class="slider-track">
    <!-- Original slides -->
    <div class="slide"><img src="/images/br.webp" alt="br logo" /></div>
    <div class="slide"><img src="/images/dcc.webp" alt="dcc logo" /></div>
    <div class="slide"><img src="/images/myselfie.webp" alt="myselfie logo" /></div>
    <div class="slide"><img src="/images/abg.webp" alt="alm brand logo" /></div>
    <div class="slide"><img src="/images/ecograf.webp" alt="ecograf logo" /></div>
    <div class="slide"><img src="/images/kidvertising.webp" alt="kidvertising logo" /></div>
    <div class="slide"><img src="/images/andersenheegaard.webp" alt="andersen & heegaard logo" /></div>
    <div class="slide"><img src="/images/kiplingtravel.webp" alt="kipling travel logo" /></div>
    <div class="slide"><img src="/images/grundfos.webp" alt="grundfos logo" /></div>
    <div class="slide"><img src="/images/kadaver.webp" alt="kadaver logo" /></div>
    <div class="slide"><img src="/images/skoda.webp" alt="skoda logo" /></div>
    <div class="slide"><img src="/images/danskefragtmaend.webp" alt="danske fragtmænd logo" /></div>
    <div class="slide"><img src="/images/continental.webp" alt="continental logo" /></div>
    <div class="slide"><img src="/images/djurslandsbank.webp" alt="djurslands bank logo" /></div>
    <div class="slide"><img src="/images/smykbar.webp" alt="smykbar logo" /></div>
    <div class="slide"><img src="/images/ocean.webp" alt="ocean logo" /></div>
  </div>
</div>

<style>
  .slider-container {
    overflow: hidden;
    width: 100vw;
    white-space: nowrap;
  }

  .slider-track {
    display: flex;
    gap: 2rem;
    will-change: transform;
  }

  .slide {
    flex: 0 0 auto;
    width: 10rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .slide img {
    width: 10rem;
    height: auto;
  }
</style>

<script>
  import { inView } from "motion";

  document.addEventListener("DOMContentLoaded", function () {
    const track = document.querySelector(".slider-track");
    const slides = Array.from(track.children);

    // Duplicate slides to ensure smooth infinite loop
    slides.forEach((slide) => {
      const clone = slide.cloneNode(true);
      track.appendChild(clone);
    });

    let speed = 1; // Adjust speed if needed
    let position = 0;

    function animate() {
      position -= speed;

      // Move first slide to the end instead of resetting
      if (position <= -slides[0].offsetWidth - 32) {
        // Adjust for gap (2rem = 32px)
        position = 0;
        track.appendChild(track.firstElementChild);
      }

      track.style.transform = `translateX(${position}px)`;
      requestAnimationFrame(animate);
    }

    animate(); // Start animation
  });

  /* inView Animations */
  const elemntmedanimation1 = document.querySelector(".slider-container");
  inView(elemntmedanimation1, () => {
    elemntmedanimation1.classList.add("animate__animated", "animate__zoomIn");
  });
</script>
